apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cryptpad-helm.fullname" . }}-test-connection"
  labels:
    {{- include "cryptpad-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cryptpad-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
